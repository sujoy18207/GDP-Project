# Core web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Data + ML
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2

# Fairness libraries
# NOTE: aif360 has heavy native dependencies (cvxpy, tensorflow, etc.)
# We rely on fairlearn (pip-friendly) and implement core metrics directly.
fairlearn==0.11.0

# PDF report generation
reportlab==4.2.5

# Persistence + serialization
joblib==1.4.2
pydantic==2.9.2
